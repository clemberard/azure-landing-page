{
    "name": "neonarena",
    "version": "1.0.0",
    "description": "A minimal React application in a single HTML file for Azure deployment",
    "main": "index.html",
    "scripts": {
      "start": "serve -s .",
      "build": "mkdir -p build && cp index.html build/",
      "deploy": "az staticwebapp up --name my-react-app --resource-group myResourceGroup --source . --output-location build"
    },
    "dependencies": {
      "serve": "^14.2.3"
    },
    "devDependencies": {},
    "engines": {
      "node": ">=18.x"
    }
  }